{
  "name": "ibe260_oblig2_simple",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon -e ts --watch src --exec \"npm run compile\" || exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist/*",
    "compile": "tsc --outDir dist/ && node dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "jsonstat-toolkit": "^1.4.2",
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "@types/node-fetch": "^2.6.11",
    "typescript": "^5.4.5"
  }
}
